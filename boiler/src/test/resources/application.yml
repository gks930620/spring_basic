spring:
  datasource:
    url: jdbc:mariadb://localhost:3406/doll_gacha
    username: doll_gacha
    password: ckdlsk13!
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop # 테스트 시작 시 생성, 종료 시 삭제
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        default_batch_fetch_size: 100
    open-in-view: false

  sql:
    init:
      mode: always
      data-locations:
        - classpath:data-users.sql
        - classpath:data-dollshop.sql
        - classpath:data-files.sql
        - classpath:data-review.sql

  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: test-kakao-id
            client-secret: test-kakao-secret
            client-authentication-method: client_secret_post
            redirect-uri: http://localhost:8080/login/oauth2/code/kakao
            authorization-grant-type: authorization_code
            scope:
              - profile_nickname
              - account_email
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

jwt:
  secret: testSecretKeyShouldBeLongEnoughToSatisfyRequirements1234567890
  expiration_access: 1800000
  expiration_refresh: 14400000
